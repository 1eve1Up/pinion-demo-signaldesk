# Copy to `.env` in this directory or export these variables in your shell.
# Values below are placeholders only; use a strong secret in real environments.

DATABASE_URL=sqlite+aiosqlite:///./signaldesk.db
JWT_SECRET_KEY=replace-with-a-long-random-secret-suitable-for-hs256
